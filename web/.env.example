# Registry types offered by the project create/edit forms. The instance's
# knownRegistries set is not exposed by an endpoint, so the UI reads this
# build-time env (comma-separated).
VITE_KNOWN_REGISTRIES=npm,pypi,maven,goproxy
# Optional base URL for the admin API (defaults to same-origin).
# VITE_API_BASE=
